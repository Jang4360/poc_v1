{
  "priority": [
    { "if": "walk_access == NO", "multiply_by": "0" },
    { "if": "stairs_state == YES", "multiply_by": "0" },
    { "if": "width_state == NARROW", "multiply_by": "0.35" },
    { "if": "surface_state == UNPAVED", "multiply_by": "0.55" },
    { "if": "avg_slope_percent >= 8.33", "multiply_by": "0.45" }
  ],
  "speed": [
    { "if": "true", "limit_to": "foot_average_speed" }
  ],
  "distance_influence": 45
}
